"""
Калейдоскоп. Печенин Данила БПМ233.

1. Элементы анимации
1.1. Фон
Фон заполнен радужным градиентом, цвет которого зависит от полярного угла
в калейдоскопном пространстве и медленно вращается во времени. Поверх фона
нанесен спиральный полосатый узор: полосы двигаются по спирали от центра
к краям.

1.2. Волновое искажение пространства
Все UV-координаты перед обработкой проходят нелинейное искажение: каждая
ось сдвигается на величину, пропорциональную синусу/косинусу перпендикулярной
оси и времени.

1.3. Масштаб и глобальное вращение
Сцена медленно пульсирует по масштабу через sin(t) и непрерывно
вращается как единое целое с фиксированной скоростью.

1.4. Калейдоскопное складывание
Пространство делится на 6 равных угловых секторов через операцию fract
над полярным углом. Внутри каждого сектора применяется зеркальное
отражение относительно биссектрисы, создавая идеальную 6-лучевую симметрию.

1.5. Центральный пульсирующий круг (SDF)
В центре каждого лепестка находится круг, радиус которого пульсирует
по синусоидальному закону. Цвет круга вычисляется косинусной палитрой.

1.6. Орбитальный прямоугольник (SDF)
Прямоугольник движется по орбите вокруг центра и одновременно вращается
вокруг своей оси. Ширина пульсирует во времени.

1.7. Орбитальный треугольник (SDF)
Равносторонний треугольник движется по орбите в противоположном направлении
от прямоугольника и также вращается вокруг своей оси.

1.8. Внешние малые круги (SDF)
Кольцо малых кругов на дальней орбите, радиус каждого пульсирует с
высокой частотой (мигает).

1.9. Плавное объединение форм (smoothmin)
Все четыре SDF-формы объединяются через функцию smoothmin: вместо
острых пересечений получаются плавные «сплавления».

1.10. Ореол и виньетка
Вокруг каждой формы строится мягкий ореол через smoothstep по расстоянию
от поверхности. По краям кадра применяется виньетка через smoothstep по
расстоянию от центра экрана.

2. Функции и ссылки на Graphtoy
Ниже независимая переменная x используется как удобная замена для
соответствующей величины из шейдера: времени t, расстояния r или параметра d.

2.1. Пульсация масштаба сцены
Формула: z(t) = 1.15 + 0.12 * sin(0.7 * t)
Ссылка:
https://graphtoy.com/?f1(x,t)=1.15+0.12*sin(0.7*x)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,1.15,12

2.2. Радиус центрального пульсирующего круга
Формула: r(t) = 0.07 + 0.025 * sin(3.1 * t)
Ссылка:
https://graphtoy.com/?f1(x,t)=0.07+0.025*sin(3.1*x)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,0.07,8

2.3. Косинусная цветовая палитра (один канал, a=b=0.5, c=1, d=0)
Формула: c(t) = 0.5 + 0.5 * cos(2 * pi * t)
Ссылка:
https://graphtoy.com/?f1(x,t)=0.5+0.5*cos(2*PI*x)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,0.5,4

2.4. Ореол по расстоянию от SDF-поверхности
Формула: glow(d) = 1 - smoothstep(d, 0, 0.12)
Ссылка:
https://graphtoy.com/?f1(x,t)=1-smoothstep(x,0,0.12)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,0.5,0.5

2.5. Виньетка по расстоянию от центра экрана
Формула: v(r) = smoothstep(r, 0, 0.65)
Ссылка:
https://graphtoy.com/?f1(x,t)=smoothstep(x,0,0.65)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,0.5,1.5

2.6. Спиральная фаза полосы
Формула: s(r, θ, t) = fract(r * 6 - θ / pi - t * 0.6)
Ссылка (при фиксированных θ=0, t=0): fract(6*x)
https://graphtoy.com/?f1(x,t)=fract(6*x-t*0.6)&v1=true&f2(x,t)=&v2=false&f3(x,t)=&v3=false&f4(x,t)=&v4=false&f5(x,t)=&v5=false&f6(x,t)=&v6=false&grid=1&coords=0,0.5,2

3. Взаимосвязи элементов
Сначала UV проходит волновое искажение, затем масштабирование и глобальное
вращение. После этого пространство складывается в калейдоскоп (6 секторов +
зеркальное отражение). В сложенном пространстве вычисляются SDF четырех
фигур (круг, прямоугольник, треугольник, внешние круги), которые объединяются
через smoothmin. Фон строится через радужный градиент по полярному углу плюс
спиральный узор через fract. Поверх фона накладывается ореол форм, затем
сами формы и наконец виньетка.

4. Идея реализации
- волновое искажение делает сцену живой без анимации самих форм
- калейдоскопное складывание через fract полярного угла позволяет описать
  всю сцену в одном секторе и получить N-лучевую симметрию без повторов
- зеркальное отражение внутри сектора делает узор
  непрерывным на границах секторов
- все фигуры задаются SDF и объединяются через smoothmin, что дает
  плавные, органичные переходы между ними
- цвета вычисляются косинусной палитрой, зависящей от значения SDF,
  что автоматически создает цветовые кольца вокруг каждой формы
"""

import taichi as ti
import taichi.math as tm
import time


@ti.data_oriented
class KaleidoscopeShader:
    """
    Шейдер «Калейдоскоп».

    Реализует интерактивный калейдоскоп с динамическими геометрическими
    фигурами, вращающимися и пульсирующими во времени.

    Реализованные визуальные элементы:
      1.  Волновое искажение UV-координат          (f(x,t) + нелинейное)
      2.  Пульсация масштаба сцены                 (f(x,t))
      3.  Глобальное вращение сцены                (линейное преобразование)
      4.  Калейдоскопное складывание (6 секторов)  (нелинейное: радиальное повторение)
      5.  Отражение внутри сектора                 (нелинейное: отражение (зеркало))
      6.  Вращение внутри сложенного пространства  (линейное преобразование)
      7.  Центральный пульсирующий круг            (SDF круг + f(x,t))
      8.  Орбитальный вращающийся прямоугольник    (SDF прямоугольник + линейное)
      9.  Орбитальный вращающийся треугольник      (SDF треугольник + линейное)
      10. Кольцо малых кругов                      (SDF круг + f(x,t))
      11. Спиральный полосатый узор                (полярные координаты + f(x,t))
      12. Плавное объединение форм (smoothmin)     (сглаживающая функция)
      13. Ореол вокруг форм                 (smoothstep)
      14. Радужный фоновый градиент                (палитра цветов + mix)
      15. Косинусная палитра для фигур             (цветовое смешивание)
      16. Виньетка по краям                        (smoothstep)
    """

    def __init__(self, title: str, res: tuple[int, int] | None = None, gamma: float = 2.2):
        """
        Инициализация шейдера.

        :param title: заголовок окна
        :param res: разрешение окна (ширина, высота) в пикселях
        :param gamma: показатель гамма-коррекции (0 — отключить)
        """
        self.title = title
        self.res = res if res is not None else (1000, 563)
        self.resf = tm.vec2(*self.res)
        self.pixels = ti.Vector.field(3, dtype=ti.f32, shape=self.res)
        self.gamma = gamma

    # Вспомогательные математические функции (из core.py)

    @ti.func
    def smoothmin(self, a: ti.f32, b: ti.f32, k: ti.f32) -> ti.f32:
        """
        Сглаживающий минимум (polynomial smooth min).

        [Сглаживающие функции] — плавное объединение SDF-форм без
        острого шва, который дает обычный min().

        :param a: первое значение
        :param b: второе значение
        :param k: радиус сглаживания (чем больше — тем мягче)
        :return:  сглаженный минимум двух значений
        """
        h = max(k - abs(a - b), 0.0) / k
        return min(a, b) - h * h * k * 0.25

    @ti.func
    def rot2(self, angle: ti.f32) -> tm.mat2:
        """
        Матрица поворота на заданный угол (против часовой стрелки).

        [Линейные преобразования] — стандартная матрица поворота 2×2.

        :param angle: угол поворота в радианах
        :return:      матрица поворота tm.mat2
        """
        c = ti.cos(angle)
        s = ti.sin(angle)
        return tm.mat2([c, -s], [s, c])


    # SDF-примитивы

    @ti.func
    def sd_circle(self, p: tm.vec2, r: ti.f32) -> ti.f32:
        """
        SDF круга с центром в начале координат.

        [SDF — примитив 1: круг]

        :param p: координата точки
        :param r: радиус круга
        :return: знаковое расстояние (< 0 — внутри, > 0 — снаружи)
        """
        return p.norm() - r

    @ti.func
    def sd_box(self, p: tm.vec2, b: tm.vec2) -> ti.f32:
        """
        SDF оси-выровненного прямоугольника с центром в начале координат.

        [SDF — примитив 2: прямоугольник]

        :param p: координата точки
        :param b: полуразмеры прямоугольника (half-width, half-height)
        :return: знаковое расстояние
        """
        d = ti.abs(p) - b
        return ti.max(d, 0.0).norm() + ti.min(ti.max(d.x, d.y), 0.0)

    @ti.func
    def sd_triangle(self, p: tm.vec2, r: ti.f32) -> ti.f32:
        """
        SDF правильного (равностороннего) треугольника.

        [SDF — примитив 3: треугольник]

        :param p: координата точки
        :param r: «радиус» описывающего треугольник размера
        :return: знаковое расстояние
        """
        k = ti.sqrt(3.0)
        # Отражаем x для симметрии, смещаем y
        pp = tm.vec2(ti.abs(p.x) - r, p.y + r / k)
        # Срезаем угол
        if pp.x + k * pp.y > 0.0:
            pp = tm.vec2(pp.x - k * pp.y, -k * pp.x - pp.y) * 0.5
        pp.x -= tm.clamp(pp.x, -2.0 * r, 0.0)
        return -pp.norm() * tm.sign(pp.y)


    # Функции цвета

    @ti.func
    def hue_gradient(self, t: ti.f32) -> tm.vec3:
        """
        Полный спектр оттенков (HSV-подобный градиент).

        [Смешивание цветов — палитра оттенков]

        :param t: параметр [0, 1]
        :return: цвет RGB
        """
        p = ti.abs(tm.fract(t + tm.vec3(1.0, 2.0 / 3.0, 1.0 / 3.0)) * 6.0 - 3.0)
        return tm.clamp(p - 1.0, 0.0, 1.0)

    @ti.func
    def cosine_palette(self, t: ti.f32, a: tm.vec3, b: tm.vec3, c: tm.vec3, d: tm.vec3) -> tm.vec3:
        """
        Косинусная цветовая палитра.

        color(t) = a + b · cos(2π · (c·t + d))

        [Смешивание цветов — косинусная палитра]
        [Функции f(x,t) — косинус для плавного расчета цвета]

        :param t: параметр анимации
        :param a: смещение (offset)
        :param b: амплитуда (amplitude)
        :param c: частота (frequency)
        :param d: фазовый сдвиг (phase)
        :return: цвет RGB
        """
        return a + b * tm.cos(2.0 * tm.pi * (c * t + d))


    # Пространственные преобразования

    @ti.func
    def wave_distort(self, p: tm.vec2, t: ti.f32) -> tm.vec2:
        """
        Нелинейное волновое искажение координат.

        [Нелинейные преобразования — искажение через sin/cos]
        [Функции f(x,t) — синусоидальная волна]

        Амплитуда искажения зависит от расстояния до центра и времени:
        каждая координата сдвигается на волновую величину, зависящую
        от перпендикулярной оси и времени.

        :param p: исходные координаты точки
        :param t: текущее время
        :return: искаженные координаты
        """
        r = p.norm()
        amp = ti.sin(r * 7.0 - t * 2.5) * 0.022
        return p + tm.vec2(
            ti.sin(p.y * 5.5 + t * 1.7) * amp,
            ti.cos(p.x * 5.5 + t * 1.7) * amp,
        )

    @ti.func
    def kaleidoscope_fold(self, p: tm.vec2, n: ti.f32) -> tm.vec2:
        """
        Нелинейное преобразование: радиальное повторение + зеркальное отражение.

        [Нелинейные преобразования — радиальное повторение]
        [Нелинейные преобразования — отражение]

        Делит плоскость на n равных угловых секторов и складывает их
        поверх друг друга, создавая калейдоскопную симметрию.
        Внутри каждого сектора применяется зеркальное отражение.

        :param p: точка в 2D пространстве
        :param n: количество лепестков (секторов)
        :return: трансформированная точка
        """
        r = p.norm()
        # Полярный угол в диапазоне [-π, π]
        angle = ti.atan2(p.y, p.x)
        sector = 2.0 * tm.pi / n

        # Радиальное повторение: fract переводит угол в [0, sector)
        a = tm.fract((angle + tm.pi) / sector) * sector

        # Отражение внутри сектора: складываем вторую половину
        half = sector * 0.5
        a = half - ti.abs(a - half)

        return tm.vec2(ti.cos(a), ti.sin(a)) * r


    # Основная функция рендеринга

    @ti.func
    def main_image(self, uv: tm.vec2, t: ti.f32, cursor: tm.vec2) -> tm.vec3:
        """
        Вычисляет цвет одного пикселя — основная логика калейдоскопа.

        Курсор мыши управляет цветовым сдвигом (ось X).

        :param uv: нормализованные координаты пикселя (центр = (0, 0))
        :param t:  время в секундах от начала анимации
        :param cursor: позиция курсора [0,1]×[0,1]
        :return: цвет пикселя RGB (линейное пространство)
        """

        # cursor.x → смещение оттенка
        hue_shift = cursor.x * 0.6

        # 1. Волновое искажение UV-координат — нелинейные + f(x,t)
        p = self.wave_distort(uv, t)

        # 2. Пульсация масштаба сцены — f(x,t), масштабирование через sin
        zoom = 1.15 + 0.12 * ti.sin(t * 0.7)
        p = p * zoom

        # 3. Глобальное вращение сцены — линейные преобразования, матрица поворота
        p = self.rot2(t * 0.12) @ p

        # 4, 5. Калейдоскопное складывание (6 секторов) + зеркальное отражение — нелинейные
        pk = self.kaleidoscope_fold(p, 6.0)

        # 6. Вращение внутри сложенного пространства — линейные преобразования
        p_inner = self.rot2(t * 0.9) @ pk

        # 7. Центральный пульсирующий круг — SDF круг + f(x,t)
        r_center = 0.07 + 0.025 * ti.sin(t * 3.1)
        d_center = self.sd_circle(p_inner, r_center)

        # 8. Орбитальный вращающийся прямоугольник — SDF прямоугольник + линейные
        box_orbit = 0.19
        box_angle = t * 1.4
        box_pos = tm.vec2(ti.cos(box_angle), ti.sin(box_angle)) * box_orbit  # параллельный перенос
        box_size = tm.vec2(0.045 + 0.012 * ti.sin(t * 2.3), 0.022)
        pb = self.rot2(box_angle * 2.0) @ (p_inner - box_pos)
        d_box = self.sd_box(pb, box_size)

        # 9. Орбитальный вращающийся треугольник — SDF треугольник + линейные
        tri_orbit = 0.33
        tri_angle = -t * 1.0 + tm.pi / 6.0
        tri_pos = tm.vec2(ti.cos(tri_angle), ti.sin(tri_angle)) * tri_orbit
        tri_size = 0.055 + 0.018 * ti.sin(t * 1.9 + 1.0)
        pt = self.rot2(t * 1.8) @ (p_inner - tri_pos)
        d_tri = self.sd_triangle(pt, tri_size)

        # 10. Кольцо малых пульсирующих кругов — SDF круг + f(x,t)
        outer_orbit = 0.44
        outer_angle = t * 0.55 + tm.pi / 3.0
        outer_pos = tm.vec2(ti.cos(outer_angle), ti.sin(outer_angle)) * outer_orbit
        small_r = 0.032 + 0.010 * ti.sin(t * 4.2)
        d_outer = self.sd_circle(p_inner - outer_pos, small_r)

        # 11. Спиральный полосатый узор на фоне — нелинейные (полярные + fract) + f(x,t)
        pr = pk.norm()
        pa = ti.atan2(pk.y, pk.x)
        spiral_phase = pr * 6.0 - pa / tm.pi - t * 0.6  # радиус + угол = скользящие полосы
        spiral_v = tm.fract(spiral_phase)
        stripe = (tm.smoothstep(spiral_v, 0.42, 0.50) - tm.smoothstep(spiral_v, 0.50, 0.58))  # сглаживающие

        # 12. Плавное объединение всех форм — сглаживающие (smoothmin)
        d = self.smoothmin(d_center, d_box, 0.035)
        d = self.smoothmin(d, d_tri, 0.025)
        d = self.smoothmin(d, d_outer, 0.018)

        # 13, 14. Радужный фоновый градиент + спиральная полоска — смешивание цветов
        bg_hue = pa / (2.0 * tm.pi) + 0.5 + pr * 0.25 + t * 0.08 + hue_shift
        bg_col = self.hue_gradient(bg_hue) * 0.28
        stripe_col = self.hue_gradient(bg_hue + 0.5) * 0.55
        bg_col = tm.mix(bg_col, stripe_col, stripe * 0.75)  # mix()

        # 15. Косинусная палитра для фигур — смешивание цветов
        shape_t = d * 6.0 + t * 0.35 + pr * 1.5 + hue_shift
        shape_col = self.cosine_palette(
            shape_t,
            tm.vec3(0.55, 0.55, 0.55),
            tm.vec3(0.50, 0.50, 0.50),
            tm.vec3(1.00, 1.00, 0.80),
            tm.vec3(0.00, 0.25, 0.55),
        )

        # 16. Ореол вокруг форм — сглаживающие (smoothstep)
        px_size = 0.003
        shape_mask = 1.0 - tm.smoothstep(d, -px_size, px_size)
        glow_mask = 1.0 - tm.smoothstep(d, 0.0, 0.12)
        glow_col = shape_col * glow_mask * 0.45

        # 17. Виньетка — сглаживающие (smoothstep по расстоянию от центра)
        vignette = tm.smoothstep(uv.norm(), 0.0, 0.65)
        vignette = 0.25 + 0.75 * vignette

        # финальная композиция — mix() для наложения слоев
        col = bg_col + glow_col
        col = tm.mix(col, shape_col, shape_mask)
        col = col * vignette

        return col


    # Ядро рендеринга и главный цикл

    @ti.kernel
    def render(self, t: ti.f32, cursor: tm.vec2):
        """
        Taichi-ядро: вычисляет цвет каждого пикселя в параллельном режиме.

        :param t: время в секундах от начала анимации
        :param cursor: позиция курсора (нормализованная, [0,1]×[0,1])
        """
        for fragCoord in ti.grouped(self.pixels):
            uv = (fragCoord - 0.5 * self.resf) / self.resf.y
            col = self.main_image(uv, t, cursor)
            # Гамма-коррекция
            if self.gamma > 0:
                col = tm.clamp(col ** (1.0 / self.gamma), 0.0, 1.0)
            self.pixels[fragCoord] = col


    def main_loop(self):
        """
        Запускает окно и основной цикл отрисовки.

        - ESC или закрытие окна завершают программу.
        - Курсор мыши влияет на цветовой сдвиг.
        """
        gui = ti.GUI(self.title, res=self.res, fast_gui=True)
        start = time.time()

        while gui.running:
            if gui.get_event(ti.GUI.PRESS):
                if gui.event.key == ti.GUI.ESCAPE:
                    break

            t = time.time() - start
            cursor = tm.vec2(*gui.get_cursor_pos())
            self.render(t, cursor)
            gui.set_image(self.pixels)
            gui.show()

        gui.close()


def main() -> None:
    """Инициализирует Taichi и запускает шейдер калейдоскопа."""
    try:
        ti.init(arch=ti.gpu, offline_cache=False)
    except Exception:
        ti.init(arch=ti.cpu, offline_cache=False)

    shader = KaleidoscopeShader(title="Kaleidoscope", res=(1000, 563), gamma=2.2,)
    shader.main_loop()


if __name__ == "__main__":
    main()
